# Dada una lista de números, imprimir todos los números que son mayores que
# el número anterior en la lista.

# lista_numeros = [12, 54, 8, 25]
# longitud_lista = len(lista_numeros)

# contador = 1

# while(longitud_lista > contador):
#     if (lista_numeros[contador])

#     contador += 1